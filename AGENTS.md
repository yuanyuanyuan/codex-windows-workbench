# Agent Routing

For any version publication, release preparation, release note, tag, or GitHub Release task, read and obey `rules/release/RELEASE-RULES.md` before making changes or publishing artifacts.

Use the `Release` GitHub Actions workflow as the only supported tag-and-release path. Do not bypass its gates with manually created tags or releases.
