# Hooks

Windows-native safety hooks. Enable only with `-EnableSafetyHooks`.

Includes `dangerous-git.ps1` covering force push, hard reset, aggressive clean, forced checkout, amend, and interactive rebase.
