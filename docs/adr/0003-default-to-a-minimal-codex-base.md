# Default to a minimal Codex Base

The Codex Workbench MVP will install only the minimum PowerShell, source-control, Node/fnm, CLI-navigation, Codex, and managed-shell components by default. Editors, language runtimes, cloud clients, and container tooling require explicit user selection so public installation stays smaller, safer, and easier to maintain.
