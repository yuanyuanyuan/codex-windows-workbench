# Publish From Immutable Release References

Public installation instructions and release notes must identify an immutable version tag or commit SHA rather than a default branch. This makes the executed Workbench source auditable and reproducible for each release, while leaving the default branch available for ongoing development.
